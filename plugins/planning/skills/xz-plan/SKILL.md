---
name: xz-plan
description: 创建新版本计划（目标/上下文/约束/完成标准 + 手动执行）。/xz-plan N 需求描述
disable-model-invocation: true
argument-hint: "[N] [需求描述]"
---

# XZ Plan - 创建新版本计划

参数 `$ARGUMENTS`：第一个空格前为版本号 N，之后为需求描述（`2 支持多语言` → N=2）。
任一不满足 → 停止并提示 `用法: /xz-plan N 需求描述`：`$ARGUMENTS` 为空 / N 非合法版本号（正整数，或小数如 1.5） / 需求为空。

**脚本**：`xz-tools.py`（`bin/` 已在 PATH，直接调用，在当前目录操作 `.xz_planning/`）。

## 前置检查

1. 无 `.xz_planning/` → 停止，提示 `请先执行 /xz-init`。
2. `xz-tools.py parse N`：`ok:true` → 停止，提示用 `/xz-update-plan N`；否则新建（`phase` 存在则复用目录）。

---

## 总纲：事实归 AI，拍板归用户

**代码、配置、文档里查得到的，一律自己查完再说；只有需要用户拿主意的才问。** 凡是写「我找过」，必须是真查过的文件，不许编。这条管全流程——调研、澄清、风险排查、测试资料，一处都不例外。

## 1. 调研（必做，不可跳过）

用 Glob/Read 深入需求模块，识别新建/修改文件与依赖。扫描排除 `.xz_planning/` 和 `.gitignore` 项。

**测试上下文扫描（必做，结果暂存喂给第 4.5 步的「测试环境与命令」段，本步不输出）**：读需求模块的同时顺带摸清「这个项目怎么跑测试」，供 4.5 写出真能执行的命令，禁止到 4.5 凭空编命令。**只摸清「怎么跑」，不定「测什么」**——具体用例是 `/xz-exec` 3.5 的事。扫描内容：
- **已有测试文件**：Glob `**/test_*.py`、`**/*_test.py`、`**/*.test.ts`、`**/*.spec.ts`、`**/__tests__/*`、`**/*_test.go`、`**/tests/**`、`**/spec/**`
- **框架与运行命令**：找到测试文件则读 1-2 个代表性文件，识别 pytest / unittest / jest / vitest / go test / 其他，以及实际运行命令（`pytest -v`、`npm test`、`go test ./...`）和可复用的 fixtures/mock/测试数据库
- **测试配置**：`pytest.ini`、`pyproject.toml`、`setup.cfg`、`jest.config.*`、`vitest.config.*`、`Makefile`（test target）、`docker-compose.yml`（测试依赖服务）
- **服务启动方式**：入口文件、启动命令、默认端口、依赖的数据库/缓存怎么起
- **无任何测试框架**：同样记录下来，第 4.5 步走临时脚本路线（脚本落在 `.xz_planning/phases/N.xxx/tests/`）
- **本次改动会碰到的外部依赖**（DB / 缓存 / MQ / 第三方 API / 测试账号）：**先自查**上面那些配置文件里有没有现成的连接与凭据——**找得到的记下来源**（4.5 只写来源不抄明文），**找不到的攒着，第 3 步澄清时一并问**，不要拖到草案之后

**风险逐项排查（必做，结果暂存喂给第 3.5 步，本步不输出）**：读完代码后，对照第 3.5 步的风险清单（安全 / 性能 / 数据兼容 / 并发时序 / 错误处理 / 边界 / 语言专项）逐项过一遍本次改动，记下命中的点，留到澄清完毕后的 3.5 步逐项与用户确认处理方式。本步只排查、不提问、不输出。

## 2. 范围检查

- **多个独立子系统（无依赖）** → 停止，输出拆分建议，各走独立 `/xz-plan`。
- **Bug/排查类**（报错、异常、贴了堆栈/截图）→ 停止，提示先用 `xz-debug` 定位根因，**禁止凭猜测规划修复**。（性能/UX/重构/新增功能不算排查类。）
- **宽重构**（改列名、改共享类型、改接口签名、批量重命名）→ **不拆成多个 `/xz-plan`**，在本计划里按下方 expand-contract 三段排 todolist。

### 宽重构：expand-contract 三段

**怎么判定**：改动本身是机械的，但改一个地方整个代码库都得跟着动，动一处就同时改坏一大片调用方，怎么切都没法让中间状态跑通。区别于「多个独立子系统」——那是几件互不相干的事，这是一件事扇到处都是。

**怎么排**（先加后删）：

1. **expand（加新的）** — 新旧两种形式并存，一个调用方都不破坏，占 1 条 todo
2. **migrate（迁调用方）** — 按包按目录分批迁，每批 1 条 todo，批与批之间代码库始终能编译能跑
3. **contract（删旧的）** — 确认没调用方了再删掉旧形式，占 1 条 todo，排在所有 migrate 批次后面

第 4 步按这个顺序排 todolist，并**必须在 PLAN 的 `## 技术方案` 里写明「本计划走 expand-contract 三段」**——`/xz-exec` 3.5 靠这句话识别宽重构，然后给它现场生成的用例表加一列「阶段」，标清哪些用例这阶段本来就不该跑、哪些必须一直绿。不写这句话，exec 每轮全量回归会把正常的中间状态判成失败，三轮上限直接撞满。

> ⚠️ **全程禁用 AskUserQuestion 等提问工具**：Claude Code 已知 bug——同一轮回复中提问弹窗之前的文本会被终端吞掉，用户会看不到理解/澄清/草案。所有确认一律用纯文本提问，等用户回复。

## 3. 理解 + 澄清（同一条回复批量输出，末尾一次提问；答复后有新问题继续追问）

### 分轮总规矩（决定什么问题放哪一轮）

**每一轮只问那些前置已经定了的问题。** 某个问题的答案得等本轮另一个问题先定下来，它就归下一轮。用户答完，重新看一遍哪些问题现在能问了，再出下一轮；没有能问的了，澄清就结束。

- **默认分两层**（最常见的情况）：实施方案 / 技术方向 / 需求约束 → 第一轮（本步）；开发风险 / 测试前置信息 → 第二轮（3.5 步）。因为要连什么、要什么账号、有什么坑，全取决于方案选哪个，方案没定就问必然问不准。
- **该合并就合并**：如果这次改动的风险和要什么测试资料，跟方案选哪个压根没关系（纯文案调整、只有一种合理改法的单点修改都算），就并进第一轮，别硬凑出一轮来。合并时在段首说一句「本次风险与方案选择无关，合并本轮」。
- **该再分就再分**：某个答复引出的新问题，如果它的答案又得等本轮别的未定项，那就放到再下一轮，别硬塞进当前这轮。
- 每轮编号都从 1 重新连号，段首注明第几轮和触发原因。**带着未定项进第 4 步一律算违规。**

调研后在**同一条回复**里依次输出 一、我的理解 → 二、未澄清清单，中间不插工具调用，末尾只问一次。**不单独输出方案对比段**——实施方案/技术方向选择本质也是「让用户在 a/b 里选」，直接作为未澄清清单里的一项混排。**已确认/能从代码稳定推断的关键点不单列**，直接并入「我的理解」的 约束/完成标准（不设「已澄清清单」段）。

> 未澄清清单**整段从 1 连续编号**（方案项 + 约束项混排、按高/中/低排序），用户末尾一次按编号回 `1a 2b 3a` 即可。**开发风险默认不在本轮**——风险依附于具体实现，方案没定查不准，按分轮总规矩落到 3.5；只有确认风险与方案选择无关时才允许合并进来。

### 一、我的理解（四段，与最终 PLAN 同构）

```markdown
## 我的理解
- **目标**：构建/修改什么、解决什么问题、期望效果
- **上下文**：涉及文件（@路径）、文件夹、文档、错误日志
- **约束**：架构、标准、安全、编码规范、兼容性
- **完成标准**：怎样算做完（测试通过 / 接口返回 X / bug 不复现）
```

### 二、未澄清清单（实施方案/技术方向选择 + 需求约束；按高/中/低排序；无则整段省略）

**段首先给一句整体状态摘要 + 一张总览表**（排版规范与 3.5 步同构，见那一节的五段骨架）：

摘要句说全局进度——哪些已定（从代码/用户已明确的，已并入「我的理解」）、还剩哪几项待定。例：`状态：缓存 key 已定，剩缓存存储选型、订单状态枚举、停机窗口 3 项待你确认。`

紧接着给逐项总览表，**回复行把推荐组合的等价展开写出来**，用户扫完表就能回：

```
未澄清清单  →  回「全部默认」= 1a 2a 3a

| # | 优先级  | 一句话              | 推荐动作        |
|---|---------|--------------------|-----------------|
| 1 | 🔴 高   | 改法选最小改动还是归一 | a 最小改动      |
| 2 | 🟠 中   | 订单状态枚举含不含退款 | a 按现有表三值  |
| 3 | 🟡 低   | 停机窗口放哪个时段    | a 凌晨 2-4 点   |
```

优先级用 `🔴 高` / `🟠 中` / `🟡 低`（emoji 与文字都要，emoji 是终端里的扫读锚点）；「一句话」≤20 字、只说问题是什么不说怎么办；「推荐动作」是 `a` 选项的 3-6 字短名。**只有 1-2 项且都是简单项时省掉总览表**，摘要句 + 清单就够。

把两类「需要用户在 a/b 里定夺」的点合到这一张清单、**整段从 1 连续编号**，单项选项 ≤4：
- **实施方案/技术方向选择**：库/框架/架构/改法有多个可行方向、会左右改动范围与开发量的，列出让用户选。选项来自代码分析、不凭空凑数，各选项须有实质差异，**`代价` 字段给量化**（如 `改 2 文件约 20 行`），禁"性能好"这类空话；**禁止只丢方案名词**，按下方展开格式给 做法/举例/代价/差异，做法**技术专有词与大白话并用**（术语给判断依据，大白话保证一眼看懂）。项目已固定、或只有一种合理改法的**不列**，直接写进「我的理解」的 约束。
- **需求约束**：真有歧义的约束/边界/验收解读、需求内部矛盾或与现状冲突，列出让用户定（`a` 放最可能的解读，说清依据）。

**每项格式**：`编号. 🔴 高 · 一句话问题`（≤25 字，编号与优先级和总览表逐字一致）→ 换行 `现状  当前代码/需求的实际情况`（带 `file:line`）→ 选项。**推荐项固定放 `a` 位、标记写在方案名之后（`✅推荐`）**，保证用户回 `全部默认` = 全选 a 即最优组合。选项分两种写法：
- **简单项**（选项字面自明，如枚举取值、要不要兼容 null）：一行并排 `a. xxx ✅推荐  b. xxx`。
- **方案类/不直观项**（实施方案/技术方向选择，或任何选项名词字面看不懂的）：**每个选项独立成块**，三字段固定顺序固定标签，**标签后补空格让所有字段值起始列对齐**——
  ```
  a. 短方案名（≤10 字）  ✅推荐
       做法  改哪里、怎么改（准确专有词：模式/库/接口/函数名）—— 技术专有词与大白话并用
       举例  一个具体例子直观展示改动前后的效果/数据/调用方式
       代价  量化：改 X 文件约 Y 行
  b. 短方案名
       做法  …
       举例  …
       代价  …
  差异  一两句点破 a/b 的本质取舍（选 a 得到什么/放弃什么，选 b 反之）
  ```

**排版硬闸**（与 3.5 步同一套，细则见那一节）：单条 ≤20 行；每个字段值 ≤2 行、禁连续 3 行以上散文块；**量化数字必须独占 `代价` 字段**、禁埋在长句里；涉及时序/数据流可加 ≤6 行终端图示但不强制。

**优先级 + 排序（必做）**：每项打标签并**从高到低排序**——`🔴 高` = 选错会大幅改变实施方向/返工成本高（多数方案选择、核心字段定义、需求冲突）；`🟠 中` = 只影响局部实现或某分支；`🟡 低` = 边角细节。方案类多属高、自然靠前 = 先定大方向。

**重点必查（最容易被擅自决定的两处，必列不得自行定）**：
- **key·字段定义**：用户提到的字段名、参数、状态值、枚举、key 命名/格式，其**含义/类型/取值范围/单位**若没说清 → 列出问，**禁自行假定**（如「订单状态枚举有哪些？`现状  现有表仅三值，需求未提退款` → a. 按现有表 ✅推荐  b. 另含退款中」）。
- **冲突**：需求内部自相矛盾（如又要实时又要批量延迟），或需求与现有代码/数据/已确认约束相抵触 → **必须列出**，给「以谁为准」的 a/b 选项，**禁自行选边**。

完整格式示例（方案类选项按展开格式写，简单项一行并排即可）：

```
状态：缓存 key 已定，剩改法选型、订单状态枚举 2 项待你确认。

未澄清清单  →  回「全部默认」= 1a 2a

| # | 优先级  | 一句话                 | 推荐动作       |
|---|---------|-----------------------|----------------|
| 1 | 🔴 高   | 改法选最小改动还是归一   | a 最小改动     |
| 2 | 🟠 中   | 订单状态枚举含不含退款   | a 按现有表三值 |

────────────────────────────────
1. 🔴 高 · 改法选哪种？

   现状  解析逻辑散落 3 处、重复约 60 行（parser_a.go:20 / b.go:35 / c.go:12），两种改法均可行

   a. 最小改动  ✅推荐
        做法  在 3 处解析函数内各加新字段的分支判断，不动调用链与现有结构；大白话：哪儿缺补哪儿，不动架构
        举例  parse_order() 里加 2 行 if 就能读到新字段 refund_at，另外 2 处同样各加 2 行
        代价  改 2 文件约 20 行
   b. 结构化归一
        做法  按单一职责抽公共 parser 模块，3 处散落逻辑收敛为统一入口 parser.Parse()；大白话：把重复解析收拢成一处维护
        举例  新建 parser.go 后，3 处原本各约 20 行的解析都变成 1 行 parser.Parse(raw)
        代价  改 5 文件约 120 行
   差异  a 马上能用但重复代码还在、下次加字段要改 3 处；b 一次多花 120 行，以后每加一个字段只改 1 处

2. 🟠 中 · 订单状态枚举含不含退款？
   现状  现有表仅三值（待付/已付/已取消），需求未提退款
   a. 按现有表三值 ✅推荐（依据：需求全文未出现退款字样）  b. 另含退款中
```

铁律：只列真有歧义/真有选择空间的；用户已明确、或能从代码现状推断的**不列**（并入「我的理解」），无则整段省略。凡影响实施方向或需求解读的未确认点必须列全、打好标签、从高到低排序；开发风险一律留到 3.5 步。

### 批量提问（纯文本）

理解 + 未澄清清单输出完，同一条回复末尾一次提问——
> 请一次回复：1) 理解准确？（`理解OK` / `理解需修正：xxx`）2) 未澄清各项（含实施方案与需求约束，`全部默认`用推荐 a，或按编号 `1a 2b 3a` 逐项覆盖）。示例：`理解OK 1a 2b`。

### 澄清追问循环（答复后必做）

- 用户答复后，把已定夺项（含选定实施方向/技术栈）并入「我的理解」的 约束/完成标准；用户修正了理解 → 更新后重出「我的理解」，必要时重新对齐。
- **重新算一遍这一轮能问什么**：答复引出新歧义（如选定某方向后暴露新的待定点）、答复不完整、或与已确认内容冲突 → **继续追问**，同格式再出一轮未澄清清单（编号重新从 1 连号，段首说明第几轮追问及触发原因）。新一轮**只放前置已定的问题**，答案还得等本轮别的未定项的留到再下一轮。直到没有能问的为止。
- **禁止带着未定项进 3.5 / 第 4 步**；无剩余问题时输出「澄清完毕，进入开发风险 + 测试资料确认」并进 3.5。

## 3.5 开发风险 + 测试资料确认（澄清完毕后强制独立一轮）

**触发时机**：第 3 步（含追问轮）全部定夺之后再发这一轮——**不得跳过直接进第 4 步**；澄清未完毕前也不做，方案没定风险查不准、要什么测试资料也说不准。

**唯一能合并进第 3 步的情况**：这次改动的风险和要什么测试资料，跟方案选哪个确实无关（纯文案调整、只有一种合理改法的单点修改都算）。这时按分轮总规矩并进第 3 步那一轮，段首注明「本次风险与方案选择无关，合并本轮」，编号接着第 3 步往后连排。**判不准就别合并**，按默认走独立一轮。

本轮问两件事、**编号从 1 连续排下来、一次回完**：**一、开发风险**（下方风险清单）+ **二、测试前置信息**（跑用例要用、第 1 步自查没找到的连接与凭据）。之所以放在这里而不是第 3 步：要防什么坑、要连什么账号，**全取决于选定的实施方案**，方案没定就问必然问不准。

> **不问「打算怎么测」**：具体测哪几个点、每条用例什么命令什么断言，本 skill 一律不定、也不拿来问用户——那是 `/xz-exec` 3.5 步在动手改代码前、拿着当下真实代码现场生成的事。这里只负责把**跑测试的地基**（怎么跑、缺什么参数）问清楚。

**一、开发风险怎么做**：基于**已定夺的实施方向**对照下面的风险清单逐项排查。**命中**＝改动真实触及该点（非假想风险）＋ 用户没说清怎么处理 ＋ 不同处理方式有实质差异。命中即列，未命中维度不展示。**能推断出安全默认也不跳过**——把它作为推荐 a 摆出来让用户一次确认。

**每条命中项必须评严重度并排序**：给每条打 `🔴 严重` / `🟠 高` / `🟡 中` / `⚪ 低` 标签（emoji 与文字都要，emoji 是终端里的扫读锚点），输出时**从严重到低排序**（严重在最前）。判级标准：
- **🔴 严重** — 安全漏洞 / 数据丢失或损坏风险（如注入、鉴权绕过、重试重复写、迁移丢数据）
- **🟠 高** — 明显影响可靠性或性能（如资源泄漏、N+1、缺超时、错误被吞）
- **🟡 中** — 影响代码质量或可维护性（如边界未处理、错误丢上下文）
- **⚪ 低** — 可选优化（如日志、命名、小幅冗余）

测试前置信息项统一标 `📋 资料`，**不参与严重度排序**，整段排在风险项之后。

**风险清单（提前预防）：**
- **安全**：硬编码密钥/token、输入未校验就用（用户输入 / 外部接口返回 / URL 参数）、敏感信息泄露到日志/错误消息/响应字段、权限鉴权缺失或绕过、弱加密（MD5/SHA1 存密码）
- **性能**：循环内冗余计算、资源未释放（连接/文件句柄/监听器/定时器）、大对象拷贝、N+1 查询、缺索引大表查询 / 缺分页、goroutine 泄漏
- **数据与兼容**：新增/改字段历史数据怎么处理、接口/协议向后兼容、迁移与回滚
- **并发/时序**：共享状态无锁、重试幂等、竞态
- **错误处理**：吞异常/空 catch、错误丢上下文、失败回滚与清理
- **边界**：空值/空集合/零值/负数/超大输入
- **语言专项**（按文件后缀匹配）：Go—未检查的 error return、`net/http` 未设超时、竞态；JS/TS—`innerHTML`/`dangerouslySetInnerHTML`(XSS)、`eval`、监听器/定时器未清理；SQL—字符串拼接注入、过宽 GRANT、动态表名未白名单；Python—async 中夹同步阻塞、ORM N+1

**二、测试前置信息怎么做**：基于已定夺的实施方向，推出这次改动会碰到哪些外部依赖、执行阶段跑测试要连哪些东西，列出**第 1 步自查代码没找到的**那些。每项写「要什么（干什么用）/ 我找过哪里 / 怎么给」，**编号接着开发风险往后连排**。**只问「要连什么」，不问「要测什么」**——测哪几个点是 `/xz-exec` 3.5 的事。

- **先自查再问（见总纲）** —— 第 1 步已查过 `.env`/`.env.example`/`config/*`/`docker-compose.yml`/`Makefile`及测试 fixture/`README` 本地开发章节/项目已有测试里的连接配置。**找得到的不问**，直接写进 4.5「测试环境与命令」，只写来源不抄明文。
- **起本地依赖必须作为选项让用户选** —— 凡是"我可以 docker-compose / testcontainers 起一个"的，写成该项的候选选项并**注明占用端口、跑完是否清理**，用户选中才执行，**禁止自作主张起容器**。
- **每项末尾固定给一个 `先跳过，执行时再说` 逃生项**（与开发风险的「不处理/忽略」同构）；选了跳过的在 4.5 标「待定」，执行阶段由 `/xz-exec` 7.2.1 再问一次。

### 输出排版：卡片式（五段骨架，缺一即违规）

两段编号从 1 连着排，每段内部各自排序；每条末尾都有逃生项，放最后一个字母（已有 a/b 则为 `c`，只有 a 则为 `b`）——风险段统一写「不处理/忽略（明确接受该风险）」、资料段写「先跳过，执行时再说」。

**为什么要卡片**：这一轮是用户唯一的拍板窗口，他要在几个 a/b 之间做选择。写成散文段落会把决策依据（改多少行、多几个状态、翻几倍）埋在句子中间，用户得逐字读完才能选。定长字段 + 段首总览表让他扫一眼就能回。

**① 段首回复行 + 总览表（必给）** —— 回复行把推荐组合的等价展开直接写出来，用户不必自己数编号：

```
开发风险 + 测试资料确认  →  回「全部默认」= 1a 2a 3a

| # | 严重度  | 一句话                    | 推荐动作          |
|---|---------|--------------------------|-------------------|
| 1 | 🔴 严重 | 失败重试可能重复写         | a 仅查询类重试     |
| 2 | 🟠 高   | 新接口入参未见校验         | a 入口统一校验     |
| 3 | 📋 资料 | 测试库 DSN 代码里找不到    | a 直接贴给我       |
```

- 「一句话」≤20 字，**只说问题是什么，不说怎么办**；「推荐动作」是 `a` 选项的 3-6 字短名，不是完整句子
- **总览表是「够用就够」的出口**：用户扫完表就能回复，下面的卡片是给想细看的人准备的

**② 卡片标题行**：`N. 🔴 严重 · 一句话问题`（≤25 字），编号与严重度和总览表逐字一致。

**③ 定长字段区** —— 每行一个字段，**标签后补空格，让所有字段值起始列对齐**。风险项：

| 字段 | 写什么 | 必需 |
|---|---|---|
| `现在` | 当前代码的行为，带 `file:line` | ✅ |
| `改后` | 这次改动让它变成什么 | ✅ |
| `出事` | 触发条件 `→` 后果，用箭头连，**禁写成句子** | ✅ |
| `但现在` | 为什么当前数据形态下其实无害 / 缓解因素 | 可选 |

测试前置信息项：

| 字段 | 写什么 | 必需 |
|---|---|---|
| `用途` | **这次改动为什么会碰到它**（不写「T4 要用」——T 编号是 `/xz-exec` 3.5 才生成的） | ✅ |
| `我找过` | 查过哪些文件（带 `file:line`）`→` 结论。必须真查过，禁编（见总纲） | ✅ |
| `我能自己搞定` | 相关但不用用户给的部分（如「HTTP 端口我自己起就行」），免得用户多操心 | 可选 |

**④ 选项块** —— 每个选项独立成块，三字段固定顺序固定标签；推荐项固定 `a` 位、标记写在方案名之后：

```
a. 短方案名（≤10 字）  ✅推荐
     做法  改哪里、怎么改 —— 技术专有词与大白话并用
     代价  量化：改 X 处约 Y 行 / +1 个状态 key / 占端口 Z、跑完清理
     结果  选它之后是什么状态
b. 短方案名
     做法  …
     代价  …
     结果  …
c. 不处理/忽略（明确接受该风险）
```

**逃生项单行写完、不展开三字段**。资料项的选项若字面自明（`直接贴给我` / `指到文件字段我自己读`）也可单行，只有「我起本地实例」这类必须写 `代价`（注明占用端口、跑完是否清理）。

**⑤ 收口行**：`取舍  a 得到什么/放弃什么；b 反之` —— 一行点破本质，≤2 行。

**排版硬闸（防止又退化成散文）**：

1. **单条卡片 ≤20 行** —— 超了说明这条该简化，或本就是两条风险硬并成一条
2. **每个字段值 ≤2 行** —— 禁止出现连续 3 行以上的散文块
3. **关键数字必须独占字段**（行数、条数、端口、倍数、时长）—— 禁止埋在长句中间，这是用户做选择的唯一依据
4. **只有 1-2 条且都是简单项时省掉总览表** —— 直接一行并排 `1. 🔴 严重 失败重试可能重复写 → a. 仅查询类重试 ✅推荐  b. 写请求重试 + 幂等 key  c. 不处理/忽略（明确接受该风险）`；为 1 条风险摆张表比不摆更累
5. **终端图示允许但不强制** —— 涉及时序、多路分支、数据流时可加 ≤6 行 ASCII 图示，位置在字段区之后、选项块之前；不要求每条都画

完整示例：

```
开发风险 + 测试资料确认  →  回「全部默认」= 1a 2a

| # | 严重度  | 一句话                 | 推荐动作       |
|---|---------|-----------------------|----------------|
| 1 | 🔴 严重 | 失败重试可能重复写      | a 仅查询类重试  |
| 2 | 📋 资料 | 测试库 DSN 代码里没有   | a 直接贴给我    |

────────────────────────────────
一、开发风险

1. 🔴 严重 · 失败重试可能重复写

   现在   client.go:88 统一重试 3 次，写请求也在内
   改后   本次新增的下单接口走同一条重试链路
   出事   下单超时但服务端已落库 → 重试 → 同一订单入库 2 次
   但现在 现有写接口都是幂等的，只有新增这条不是

   a. 仅查询类重试  ✅推荐
        做法  按 HTTP 方法白名单放行重试，GET/HEAD 才重试
        代价  改 1 处约 8 行
        结果  写请求超时直接报错，由调用方决定要不要重发
   b. 写请求重试 + 业务幂等 key
        做法  下单入参加 idempotency_key，服务端按 key 去重
        代价  改 3 处约 70 行、DB 加一个唯一索引
        结果  超时也能安全重发，但要维护 key 的生成与过期
   c. 不处理/忽略（明确接受该风险）

   取舍  a 现在就堵住重复写、代价是超时要调用方自己管；b 体验更好但多一套幂等机制要维护

二、测试前置信息（跑测试要用，我自查过代码没找到）

2. 📋 资料 · 测试库 DSN

   用途   改动会往 orders 表写数据，执行阶段要写进去再查回来核对
   我找过 `.env` 无 DB_* 项 → `docker-compose.yml` 只有 app 服务 → `conftest.py` 无 fixture

   a. 直接贴给我  ✅推荐
   b. 告诉我在哪个文件哪个字段，我自己读
   c. 我用项目 docker-compose 起本地实例
        代价  占 3307 端口，跑完清理
   d. 先跳过，执行时再说
```

**空段整段省略**（不写「无」占位）：只有风险 → 只发第一段；只缺资料 → 只发第二段。**两段都空**才输出「已逐项排查安全/性能/数据兼容/并发/错误处理/边界等维度，本次改动无需额外确认的开发风险；测试所需连接与凭据都能从代码里读到」并直接进第 4 步。

**用户回复后**：
- **风险项** —— 选定的处理方式并入「我的理解」的 约束 / 技术方案，并**必须在第 4 步草案 todolist 的 改动详情 里落地**（如"入口加校验"要成为某条 todo 的具体改动点），不得只确认不落地。选「不处理/忽略」的不落地为 todo，但在 约束 里记一句「已确认接受 X 风险，本次不处理」留痕。
- **连接凭据** —— 按第 6 步规则写进 `tests/.env.local`（文件头标注勿提交 + 先确保 `.gitignore` 覆盖），**PLAN 里只写引用不写明文**；选「先跳过」的在 4.5 标「待定」。

然后进第 4 步。

## 4. 生成草案（先输出对话，不写文件）

**输出顺序（硬性，违反即重来）**：工具调用（`date`、补读代码等）全部前置做完，然后在**同一条回复**里连续输出——一、草案全文（按下方模板逐字写，含每条 todo 的 改动详情）→ 二、4.5 测试环境与前置信息 → 三、第 5 步确认提问，中间不得再插工具调用；禁止只发提问不发草案，禁止用「计划已生成」之类的摘要代替全文。

时间：`date "+%Y-%m-%d %H:%M:%S"`（在输出草案前调用）。

**章节规则**：目标 写做啥；上下文 放涉及文件 + 用户贴的代码/日志/链接（**原文保留禁止总结**），无则写「无」；约束 写约束，无则「无」；完成标准 写可验证标准。

**改动详情**：修改标 `path:起始-结束行` + `(现:摘要)`（行号必 Read 确认，禁猜）；新建不标行号但列函数/类/常量名；超 30 行拆子区域。

**粒度：竖着切一刀，别横着片一层。** 一条 todo 就是一条从入口打通到出口的完整链路（handler → service → dao → 用例一路走完），范围可以窄，但环节不能缺——做完就能单独跑起来看结果，能被一条机器可判的 T 用例盖住。

- **禁止按层切**：先把 model 全改完再改 handler 这种，单条写不出行为断言，跟 4.5「每条 todo 至少被一条用例覆盖」直接冲突。
- **纯内部逻辑的改动**（没有对外接口的工具函数、数据处理、脚本）不套分层这一说，判据换成「做完能跑出一个可以断言的结果」。
- 时间只当参考上限，单条估着超过半小时就再拆一下，**别拿时间当切分依据**。
- **宽重构是例外**：按第 2 步的 expand-contract 三段排，中间态不要求每条都能单独跑绿。

````markdown
# 版本 N: 中文需求名（10-25 字，与目录名同步）

> 创建时间: YYYY-MM-DD HH:mm:ss
> 最后更新: YYYY-MM-DD HH:mm:ss
> 状态: 待执行

## 目标
做什么、解决什么问题、期望效果。

## 上下文
> 无则写「无」。
- 相关文件/文件夹：@路径
- 引用代码/数据/日志：用户原文片段（代码块包住，禁止总结）
- 外部链接/文档、业务背景/术语

## 约束
架构、标准、安全、编码规范、兼容性。无则「无」。

## 完成标准
怎样算做完（测试通过 / 接口返回 X / bug 不复现）。

## 技术方案
澄清中选定的实施方向及理由。

## 任务清单

- [ ] 1. 任务标题

  **文件:**
  - 新建: `path`
  - 修改: `path:42-58`

  ```
  改动详情:
  新建: path → 导出的函数/常量/类型
  修改: path:42-58 (现:摘要, 改后:目标) → 具体改动点
  ```
----------------------
- [ ] 2. ...

## 总结
共 N 条，涉及 X 文件（新建 Y，修改 Z）。逐条一句：做什么、涉及文件、关键函数、改成什么样。

## 改动前后对比
基于实际代码/数据，直观展示宏观变化（有数据贴数据、有请求贴请求响应、可用终端图示），不重复 todolist。
改动前：（现状）
改动后：（变化后）

## 测试方案
### 测试环境与命令
### 测试前置信息
### 通过标准
> 三段的写法与示例见下方 4.5，草案与 PLAN 里都要写成完整内容，**一段都不能少**。
> **这里不列具体用例**——测哪几个点、什么命令什么断言，由 `/xz-exec` 3.5 步执行时现场生成。

## 变更记录
- YYYY-MM-DD HH:mm:ss 初始创建
````

## 4.5 测试环境与前置信息（对话展示 + 写入文件）

`## 测试方案` 是草案及最终 `N-PLAN.md` 的固定章节：草案里完整展示，确认后原样写入 PLAN，不得省略或只留在对话里。内容来自草案推导 + 第 1 步的测试上下文扫描。

**这一节只写跑测试的地基——怎么跑、缺什么参数、怎么算过。**

> **具体测哪几个点、每条用例什么命令什么断言，一律不在这里定**，由 `/xz-exec` 3.5 步在动手改代码前、拿着当下真实代码现场生成。
>
> **为什么不在计划阶段定用例**：计划写完到真正执行往往隔着若干轮澄清和改动（接口签名变了、todo 拆合了、依赖换了），plan 阶段拍下的用例到执行时经常已经不合适。让 exec 拿当下的真实代码现场推，比照抄一份过期方案准。

### 三段格式

```markdown
## 测试方案
### 测试环境与命令
框架 / 运行命令 / 起服务与依赖的具体命令。项目无测试框架则写明「无测试框架，用临时脚本 `.xz_planning/phases/N.xxx/tests/`」。已能从代码/配置里读到的连接与参数写在这里（**只写来源不写明文**，如「DB_DSN：见 `.env`」）。
执行阶段的测试产物（脚本/截图/请求响应样本/日志）**平铺**在 `.xz_planning/phases/N.xxx/tests/`，文件名带 `T{编号}-` 前缀；每条用例的复现命令写进 `N-TEST-REPORT.md` 的「复现步骤」段，供你日后照着手动重跑。

### 测试前置信息
> 执行阶段跑测试需要、但代码里找不到的连接与凭据。**本段是 3.5 步确认结果的回填，不是新一轮索取**——已给的值写进 `.xz_planning/phases/N.xxx/tests/.env.local`，这里只写引用、命令一律脱敏。

| # | 要什么 | 干什么用 | 状态 |
|---|--------|----------|------|
| P1 | MySQL DSN | 改动会往 orders 表写数据，要写进去再查回来核对 | ✅ 已提供（3.5 步），见 `tests/.env.local` |
| P2 | 支付沙箱 token | 验签路径要打通沙箱才能验 | ⏳ 待定——你回了「先跳过」，执行时由 `/xz-exec` 再问一次 |
| P3 | Redis 连接 | refresh token 走 Redis，要读回来核对 | ✅ 已确认起本地实例（占 6380，跑完清理），执行时才起 |

> 「干什么用」写**这次改动为什么会碰到它**，不写「T4/T5 要用」——T 编号是 `/xz-exec` 3.5 现场生成的，计划阶段还不存在。

（无则整段写「无」）

### 通过标准
`/xz-exec` 3.5 现场生成的全部自动化用例通过 + 人工介入项逐项确认 + 无阻塞项（前置信息都已就位）。
```

### 测试前置信息：只回填 3.5 的结果，不另起一轮

自查规则和索取格式都在 3.5，**这一步不许再向用户要一遍**：3.5 已经给了值的写进 `tests/.env.local`，本段只写引用；回了「先跳过」的标「待定」，留给 `/xz-exec` 7.2.1 执行时再问。

**起本地依赖必须作为选项、不能自己动手**：凡是"我可以 docker-compose / testcontainers 起一个"的，写成该项的一个候选选项，**注明占用端口和跑完是否清理**，由用户选中才执行——起容器会占端口和磁盘，可能与用户在跑的服务冲突。

### 不在这一步做的事（都归 `/xz-exec` 3.5）

- **列具体用例** —— 编号、命令、预期判定、「先跑」列，一条都不在这里定
- **识别人工介入项** —— 哪些 AI 客观做不到、要停下请人做，执行时按当下情况判
- **T 编号管理** —— 编号是产物文件名前缀（`tests/T3-run.log`）和报告小节号，由 exec 分配并跨轮次保持稳定
- **宽重构的阶段标记** —— 走第 2 步 expand-contract 的计划，只需在 `## 技术方案` 里写明这一点，exec 3.5 识别后自己给用例表加「阶段」列

## 5. 确认（纯文本提问，禁用提问工具）

在草案全文 + 测试方案之后、同一条回复末尾直接以文本提问：

> 以上计划是否确认？回复数字：
>   1) 确认写入 — 写 N-PLAN.md，等手动调 `/xz-exec N`
>   2) 确认写入并直接执行 — 写入后立即进入 `/xz-exec N` 流程（含完成后的自动闭环测试）
>   3) 手动执行 — 只写 N-PLAN.md（状态置待手动执行），改码清单交给 `/xz-manual N` 生成，AI 不碰源码
>   4) 修改意见 — 说明调整点后重出

「修改意见」→ 按反馈调整后重出草案。

**本步只兜底，不重复问**：测试前置信息已在 3.5 问过，**用户给过的绝不再问一遍**。仅当 4.5 写「测试环境与命令」时**新发现**要连的东西、且 3.5 没覆盖到，才在确认提问后紧接着补一小段：

```
另：写测试环境时又发现 1 项没有的信息，可以和上面的选择一起回——
  P4 消息队列地址（本次新增的异步下单要连它）：a. 直接贴　b. 指到文件字段　c. 先跳过，执行时再说
例：`2 P4a: amqp://...`
```

拿到后写进 `.xz_planning/phases/N.中文名/tests/.env.local`（写入规则见第 6 步）；没新项则整段省略。在 3.5 回了「先跳过」的项保持待定，执行阶段由 `/xz-exec` 再问一次。

## 6. 写入文件

目录已存在直接写 `N-PLAN.md`；否则新建 `.xz_planning/phases/N.中文名/`（`中文名` 10-25 字，含「改动对象+动作+关键差异」，禁空格与 `/\:*?"<>|`、首字符禁用数字（与版本号小数解析冲突），禁单用「优化/重构」等泛词，如 `订单表加软删除兼容取消流程`）。

**三条路径都必须把对话中展示的完整测试方案原样写进 PLAN**，写完都要跑 `xz-tools.py update-state`。

**用户已提供的测试前置信息 → 写 `tests/.env.local`（四条强制规则）**：
1. 写进 `.xz_planning/phases/N.中文名/tests/.env.local`，文件头加一行 `# 本文件含明文凭据，勿提交`。
2. **写之前先查项目 `.gitignore`**：未覆盖该路径 → 追加一行 `.xz_planning/**/tests/.env.local`，并在终端明确告知「已追加 .gitignore 规则」。
3. **只有这一个文件能存明文**。`N-PLAN.md` 里一律只写引用（如 `DB_DSN：见 tests/.env.local`），命令示例脱敏为 `mysql://root:***@localhost:3307/test`。
4. 用户选「我起本地实例」的项，此时不起——记进 PLAN 待 `/xz-exec` 执行时先告知端口再起。

分支差异：

**1) 确认写入** → 状态 `待执行` → 第 7 步。

**2) 确认写入并直接执行** → 状态 `待执行` → 直接按 `/xz-exec N` 完整流程接着执行，跳过第 7 步。

**3) 手动执行** → 只写 `N-PLAN.md`，状态设为 **`待手动执行`**（这个状态是手动路径的触发标记）→ **本 skill 到此为止：不生成 `N-MANUAL.md`、不改源码、不勾选 todo**。测试方案照写，人工改完码后一样要据此跑闭环测试。输出：
```
已写入 N-PLAN.md（待手动执行）
下一步运行 /xz-manual N —— 它会生成 N-MANUAL.md 手动清单（改动地图 + 逐块成品代码），
你照着贴完再跑 /xz-manual N 校验，最后 /xz-exec N 做闭环验证。
全程 AI 不动你的源码，除非你在校验闸门处明确同意。
```

> 手动路径的清单生成、计划变更同步、贴完后的校验闸门，**全部在 `/xz-manual`**，本 skill 不重复这套规则。

## 7. 输出（仅路径 1）

```
计划已写入: .xz_planning/phases/N.xxx/N-PLAN.md
下一步: /xz-exec N（执行）/ /xz-update-plan N（改条目）
```

---

## 红线（命中任一即违规重来；细节回看对应步骤，本表只判定不复述）

| # | 红线 | 什么算违规 | 回看 |
|---|------|-----------|------|
| 1 | 先输出、后确认、再落盘 | 草案全文没展示就弹确认，或确认前就写了文件 | 4 / 5 / 6 |
| 2 | 顶层四段缺一不可 | 目标 / 上下文 / 约束 / 完成标准 少任意一段 | 4 |
| 3 | 调研先行、方案来自代码 | 没读代码就出方案，或选项是凭空凑的 | 1 |
| 4 | 竖着切、别横着片 | todo 按层切（先全改 model 再改 handler），或哪条 todo 配不出行为用例 | 4 粒度 |
| 5 | 改动详情带行号 + 现状摘要 | 修改项没标 `path:起止行` 或行号靠猜；新建项没列函数/类/常量 | 4 |
| 6 | 上下文原文保留 | 用户贴的代码 / 日志被总结或改写 | 4 |
| 7 | 聚焦本次边界 | 写了需求外的"顺手优化"；多子系统没拆；版本已存在没导向 `/xz-update-plan`；**宽重构没走 expand-contract** | 2 |
| 8 | 规划阶段不碰源码 | 本 skill 内改了源码或勾了 todo（勾选是 `/xz-manual` 校验闸门之后的事） | 6 |
| 9 | 手动路径只写 PLAN，不越界 | 选了路径 3 却在本 skill 里生成 `N-MANUAL.md`——清单生成、同步、校验全归 `/xz-manual`，这里只置状态 `待手动执行` 并提示用户跑它 | 6 路径 3 |
| 10 | 澄清按依赖分轮、追问到没问题再规划 | 带着未定项进 3.5 或第 4 步；清单没合成一张、没打高中低、没排序、没写 `现状`、推荐项不在 `a` 位；方案类选项没按 做法/举例/代价/差异 展开或只丢个名词；key·字段定义有歧义没问、需求内部矛盾或与现状冲突没列出就自行选边 | 3 |
| 11 | 测试方案只写地基、三段齐、命令是真扫出来的 | `## 测试方案` 不足三段；「测试环境与命令」里的框架/命令是编的（第 1 步没真扫过）；凭据写了明文而非引用；**越界去列具体 T 用例、识别人工介入项或分配 T 编号**（那全是 `/xz-exec` 3.5 的事） | 4.5 |
| 12 | 先自查再问、凭据只存一处 | 「我找过」是编的；查得到的还问；用户给过的又问第二遍；自作主张起容器；明文凭据出现在 `tests/.env.local` 以外 | 总纲 / 3.5 / 6 |
| 13 | 3.5 两段齐、各自排序、都带逃生项 | 跳过 3.5 直接进草案；风险自行消化没列、没打 `🔴 严重/🟠 高/🟡 中/⚪ 低`、没排序、缺 `不处理/忽略` 逃生项；测试前置信息没写 `我找过` 或缺 `先跳过，执行时再说` 逃生项；确认的风险处理没落地到 改动详情；**多问了一段「打算怎么测」**（测试点不在本 skill 的职责内） | 3.5 |
| 14 | 确认清单按卡片式排版 | 第 3 步或 3.5 步的清单**没给段首总览表**（3 项以上时）；字段没按定长排布（该写 `现在/改后/出事` 或 `做法/代价/结果` 的写成了散文段落）；**单条超 20 行**、某个字段值连续 3 行以上；**关键数字埋在长句里**没独占 `代价` 字段 | 3 / 3.5 |
